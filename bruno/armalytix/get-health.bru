meta {
  name: Get Health
  type: http
  seq: 99
}

get {
  url: {{baseUrl}}/v1/source-of-funds/health
  body: none
  auth: inherit
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
